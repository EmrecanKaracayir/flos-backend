export interface IVenuesResData {
  readonly venueId: number;
  readonly name: string;
  readonly capacity: number;
  readonly address: string;
  readonly email: string;
  readonly imgPath: string;
}
